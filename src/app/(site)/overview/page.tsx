import OverviewPage from "@/components/site/overview-page";

export const metadata = {
  title: "Overview | Laith Assaf",
  description:
    "The whole system on one page — skills, specializations, and selected work.",
};

export default function Page() {
  return <OverviewPage />;
}
